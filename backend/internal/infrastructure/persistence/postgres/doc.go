// Package postgres implements the connection and schema management for the PostgreSQL database.
// It provides functions to create the connection pool (NewPostgresPool)
// and to ensure the schema is up-to-date (EnsureSchema).
package postgres
